# Google Colab API URL (via ngrok)
COLAB_API_URL=https://your-ngrok-url.ngrok.io

# Request timeout in seconds
REQUEST_TIMEOUT=10

# Environment (development, production)
ENVIRONMENT=development

# Enable debug logging
DEBUG=true
